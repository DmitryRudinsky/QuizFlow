package com.quizflow.api.model

enum class UserRoleDto { organizer, participant }
enum class QuestionTypeDto { text, image }
enum class AnswerTypeDto { single, multiple }
enum class SessionStatusDto { waiting, active, paused, ended }
